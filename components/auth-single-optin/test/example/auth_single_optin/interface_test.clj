(ns example.auth-single-optin.interface-test
  (:require [clojure.test :as test :refer :all]
            [example.auth-single-optin.interface :as auth-single-optin]))

(deftest dummy-test
  (is (= 1 1)))
